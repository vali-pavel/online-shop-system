apiVersion: v1
kind: Pod
metadata:
  name: user-db-pod
  labels:
    name: user-db-pod
    app: shop-service
spec:
  containers:
    - name: users-db
      image: mysql:5.6
      ports:
        - containerPort: 3306
      env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
        - name: MYSQL_DATABASE
          value: "users"
        - name: MYSQL_ALLOW_EMPTY_PASSWORD
          value: "yes"
