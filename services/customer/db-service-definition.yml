apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: customer-db-service
    app: shop-service
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    name: customer-db-pod
    app: shop-service
